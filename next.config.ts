import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Habilita o experimental serverActions (já habilitado por padrão no Next 14+)
  // Mantemos aqui como documentação
};

export default nextConfig;
